# This YAML is the interface for the Application development team and the MLOPs teams to integrate the engine
# in the cloud.

version: 0.0.1
name: Framify
Framify:
  input:
  output: Framify/output
  Log: # Add log file path > path -> ML engine persp -> /framify/exec.log, root path should be same.
